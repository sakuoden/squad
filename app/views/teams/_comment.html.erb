<% post.post_comments.each do |post_comment| %>
	<% @user = User.find(post_comment.post_comment_user_id) %>

	<%= link_to user_path(@user.id), class:"link" do %>
		<p class="comment_name_link"><%= @user.name %></p>
	<% end %>

	<span class="users_show_post_comment_font">
		<%= "　" + post_comment.post_comment %><br>
	</span>


<% end %>
